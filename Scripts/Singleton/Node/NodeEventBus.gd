extends Node

#region Fracaso Rotundo Sin uso y cringe

#enviar de aqui al nodo de Input Buffer el arbol de escenas del juego
#inutil lol, dejare esto igualmente activo por si acaso, si alguien lee esto y no hay nada puesto aqui por favor avisars
#esto no deberia guardar cosas a largo plazo :3
var a_variant: Variant # esto deberia guardar cualquier cosa
var a_dictionary: Dictionary # esto deberia guardar como diccionario guarda aqui cosas que quieras enviar como diccionario
var a_array: Array # esto deberia guardar todo tipo de variables si necesitas guardar muchas mas variables para enviarlas a algun autoload

func _signaled(): # esto deberia emitirse siempre que cualquier sennal se termina de emitir
	pass
#endregion
